gcd : Int Int -- Int
    =: [ dup 0 =
         if( drop
           , dup dip(%) swap gcd )
       ]

divides : Int Int -- Bool
        =: [ % 0 = ]

k_gcd : Int Int -- Int
      =: [ gcd ]

%foreign cabi k_gcd
