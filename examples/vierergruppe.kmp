type Element { E | A | B | C }

mult : Element Element -- Element
     =: [
        { case
            | E ->
            | A -> { case
                        | E -> A
                        | A -> E
                    }
        }
]
